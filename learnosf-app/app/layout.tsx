import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "LearnOS — Método Mari Anastacio",
  description:
    "Co-crie experiências de aprendizagem com o LearnOS, seu parceiro especializado em design instrucional baseado no método de Mari Anastacio.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="pt-BR">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
