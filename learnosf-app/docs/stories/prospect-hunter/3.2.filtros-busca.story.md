# Story 3.2: Filtros e Busca

## Status
Ready

## Executor Assignment
executor: "@dev"
quality_gate: "@ux-expert"
quality_gate_tools: ["coderabbit", "manual-ux-review"]

## Story
**As a** usuário,
**I want** filtrar meus leads por plataforma e status, e buscar por nome ou bio,
**so that** encontre rapidamente os prospects que preciso sem precisar rolar por centenas de cards.

## Acceptance Criteria
1. Filtro por plataforma: chips/botões `Todos | Instagram | LinkedIn` — atualiza lista ao clicar
2. Filtro por status: chips/botões `Todos | Novo | Contatado | Respondeu | Convertido | Descartado` — atualiza lista ao clicar
3. Campo de busca filtra por nome e bio em tempo real com debounce de 300ms
4. Filtros combinam usando lógica AND (ex: Instagram + Novo mostram apenas leads do Instagram com status Novo)
5. Contador dinâmico exibe "X leads encontrados" atualizado conforme filtros mudam
6. Os filtros são enviados como query params para `GET /api/leads` (server-side filtering)
7. Ao limpar todos os filtros, lista volta ao estado completo
8. URL atualiza com filtros selecionados (permite compartilhar/recarregar com filtros preservados)

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- **Primary Type:** Frontend
- **Secondary Type(s):** —
- **Complexity:** Low — componentes de filtro + hook de search

**Specialized Agent Assignment:**
- Primary Agents: @dev, @ux-expert
- Supporting Agents: —

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): debounce implementado, sem re-renders desnecessários
- [ ] Pre-PR (@ux-expert): chips acessíveis (keyboard navigation, aria-pressed)

**Self-Healing Configuration:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Severity Filter: CRITICAL
- CRITICAL: auto_fix
- HIGH: document_only

**CodeRabbit Focus Areas:**
- Primary: Acessibilidade dos chips de filtro (role="radio" ou botões com aria-pressed)
- Secondary: Debounce correto (useCallback + useRef para timeout), performance de re-render

## Complexity
**T-shirt:** S | **Pontos:** 2

## Business Value
Com centenas de leads, a lista sem filtros se torna inutilizável. Esta story transforma o dashboard em uma ferramenta de trabalho real — o usuário pode focar apenas nos prospects corretos no momento certo.

## Scope
**IN:**
- Chips de filtro por plataforma e status
- Campo de busca com debounce 300ms
- Filtros combinados (AND logic) via query params
- Contador de resultados
- Sincronização com URL

**OUT (não inclui esta story):**
- Filtros avançados (localização, data, seguidores)
- Ordenação de colunas
- Paginação visual (botões prev/next)
- Exportação filtrada

## Dependencies
- **Requer Story 3.1** — `LeadList`, `useLeads` e `api.js` devem existir

## Tasks / Subtasks

- [ ] 1. Implementar hook `useSearch` (AC: 3, 6)
  - [ ] Criar `client/src/hooks/useSearch.js`
  - [ ] Estado: `{ platform, status, search }` inicial do URL search params
  - [ ] `setFilter(key, value)` — atualiza filtro e URL params
  - [ ] `clearFilters()` — reseta tudo
  - [ ] Debounce de 300ms para o campo `search` (usando `setTimeout`/`clearTimeout`)
  - [ ] Disparar `useLeads.refetch(filters)` quando filtros mudam
- [ ] 2. Criar `LeadFilters.jsx` (AC: 1, 2, 7)
  - [ ] Criar `client/src/components/leads/LeadFilters.jsx`
  - [ ] Grupo de chips de plataforma com acessibilidade (`aria-pressed`, foco visível)
  - [ ] Grupo de chips de status com cores correspondentes
  - [ ] Botão "Limpar filtros" (visível apenas quando algum filtro está ativo)
- [ ] 3. Adicionar campo de busca (AC: 3, 6)
  - [ ] Input com ícone de lupa (shadcn/ui Input)
  - [ ] Placeholder: "Buscar por nome ou bio..."
  - [ ] Conectar ao hook `useSearch` com debounce
- [ ] 4. Integrar contador (AC: 5)
  - [ ] Exibir texto `"{total} leads encontrados"` usando `total` do `useLeads`
  - [ ] Atualizar quando `refetch` retorna
- [ ] 5. Sincronizar com URL (AC: 8)
  - [ ] Usar `URLSearchParams` para ler/escrever filtros na URL
  - [ ] `window.history.replaceState` para atualizar sem reload
- [ ] 6. Integrar em `LeadsPage.jsx`
  - [ ] Adicionar `LeadFilters` acima da `LeadList`
  - [ ] Passar callbacks do `useSearch` para `LeadFilters`

## Dev Notes

### Filtros como Query Params (Source: architecture/prospeccao-automatizada-architecture.md#4)
```
GET /api/leads?platform=instagram&status=Novo&search=maria&limit=50&offset=0
```

O backend já suporta estes params desde a Story 1.3. O frontend apenas precisa construir a URL corretamente.

### Debounce Pattern (sem biblioteca externa)
```javascript
// client/src/hooks/useSearch.js
const debounceRef = useRef(null);

const handleSearchChange = (value) => {
  clearTimeout(debounceRef.current);
  debounceRef.current = setTimeout(() => {
    setFilters(prev => ({ ...prev, search: value }));
  }, 300);
};
```

### Acessibilidade dos Chips (NFR WCAG AA)
Usar `<button>` com `aria-pressed="true|false"` para chips de filtro.
Alternativamente, grupo de `<input type="radio">` visualmente estilizados como chips.

### Cores dos Status (Source: Story 3.1 Dev Notes)
Reutilizar as mesmas cores definidas na Story 3.1 para badges de status.

### Nota da Story 3.1
`useLeads` já implementa `refetch(params)`. Esta story apenas precisa conectar
os filtros ao `refetch` através do `useSearch` hook.

### Testing
- Validação manual: testar cada filtro individualmente e combinado
- Verificar que debounce não dispara requisição a cada keystroke
- Verificar URL atualiza corretamente

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-03-14 | 1.0 | Story criada | River (SM) |

## Dev Agent Record
_(A ser preenchido pelo @dev durante implementação)_

### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
_(A ser preenchido pelo @qa)_
