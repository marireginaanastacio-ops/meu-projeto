# Story 2.2: Regeneração e Templates de Prompt

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "supertest", "vitest"]

## Story
**As a** usuário,
**I want** poder regenerar a mensagem de um lead com instruções diferentes e customizar o template de prompt,
**so that** ajuste o tom ou abordagem quando necessário e personalize a estratégia de comunicação.

## Acceptance Criteria
1. Endpoint `POST /api/leads/:id/regenerate-message` aceita body opcional `{ customPrompt?: string }`
2. Antes de gerar nova mensagem, a mensagem anterior é salva em `message_history` (array de objetos com `content`, `generated_at`, `prompt_used`)
3. `GET /api/config/prompt-template` retorna o template padrão atual de `data/config.json`
4. `PUT /api/config/prompt-template` aceita body `{ template: string, service_description?: string, service_name?: string }` e salva em `data/config.json` com `updated_at` atualizado
5. O template suporta variáveis `{{nome}}`, `{{bio}}`, `{{plataforma}}`, `{{service_description}}`
6. Se `customPrompt` é fornecido no body, ele substitui o template padrão apenas para esta requisição (não salva)
7. Retorna `{ success: true, data: { id, message } }` com a nova mensagem gerada

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- **Primary Type:** API
- **Secondary Type(s):** Integration
- **Complexity:** Low — extensão da Story 2.1 com histórico e config

**Specialized Agent Assignment:**
- Primary Agents: @dev
- Supporting Agents: —

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): Validar que message_history não cresce indefinidamente (limite de 10 entradas), body PUT validado

**Self-Healing Configuration:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Severity Filter: CRITICAL
- CRITICAL: auto_fix
- HIGH: document_only

**CodeRabbit Focus Areas:**
- Primary: Imutabilidade do message_history (não sobrescrever, sempre append)
- Secondary: Validação do template no PUT (deve conter pelo menos `{{nome}}`)

## Complexity
**T-shirt:** S | **Pontos:** 3

## Business Value
Permite iterar na qualidade das mensagens — fundamental para otimizar a taxa de conversão ao longo do tempo. O usuário pode testar diferentes abordagens sem perder o histórico.

## Scope
**IN:**
- Endpoint `POST /api/leads/:id/regenerate-message` com `customPrompt` opcional
- Histórico de mensagens anteriores (`message_history`)
- Endpoints CRUD de configuração de prompt (`/api/config/prompt-template`)
- Template customizável com variáveis

**OUT (não inclui esta story):**
- Interface visual de configuração de prompts (Story 3.4)
- A/B testing automatizado de mensagens
- Analytics de efetividade por template

## Dependencies
- **Requer Story 2.1** — `gemini.service.js` deve existir; estrutura do campo `message` no lead deve estar implementada

## Tasks / Subtasks

- [x] 1. Implementar `config.service.js` (AC: 3, 4)
  - [x] Criar `server/src/services/config.service.js`
  - [x] `getPromptTemplate()` — lê `data/config.json`, retorna config completa
  - [x] `updatePromptTemplate({ template, service_description, service_name })` — valida e salva
  - [x] Inicializar `JsonStore` para `data/config.json` separado do `leads.json`
- [x] 2. Criar `config.routes.js` e `config.controller.js` (AC: 3, 4)
  - [x] Route `GET /api/config/prompt-template`
  - [x] Route `PUT /api/config/prompt-template`
  - [x] Registrar em `index.js`: `app.use('/api/config', configRouter)`
- [x] 3. Implementar regeneração no `leads.controller.js` (AC: 1, 2, 6, 7)
  - [x] Função `regenerateMessage(req, res)`
  - [x] Buscar lead pelo ID — 404 se não encontrado
  - [x] Antes de gerar: se `lead.message` existe, fazer push em `message_history`
  - [x] Limitar `message_history` a 10 entradas (remover mais antigas se necessário)
  - [x] Se `customPrompt` no body → usar como prompt direto
  - [x] Senão → usar template padrão de `config.json`
  - [x] Gerar nova mensagem via `gemini.service`
  - [x] Salvar `message` e `message_history` no lead
  - [x] Route `POST /:id/regenerate-message` em `leads.routes.js`
- [x] 4. Testes (AC: 1–7)
  - [x] Testar que message_history recebe entrada anterior antes de regenerar
  - [x] Testar customPrompt substitui template padrão
  - [x] Testar GET/PUT de config template

## Dev Notes

### Config.json Schema (Source: architecture/prospeccao-automatizada-architecture.md#3)

```json
{
  "prompt_template": "string com {{variáveis}}",
  "service_description": "string",
  "service_name": "string",
  "updated_at": "ISO date"
}
```

### message_history Entry (Source: architecture/prospeccao-automatizada-architecture.md#3)

```json
{
  "content": "texto da mensagem anterior",
  "generated_at": "2026-03-14T10:00:00.000Z",
  "prompt_used": "template ou customPrompt utilizado"
}
```

### Endpoints (Source: architecture/prospeccao-automatizada-architecture.md#4)

```
POST /api/leads/:id/regenerate-message   body: { customPrompt?: string }
GET  /api/config/prompt-template
PUT  /api/config/prompt-template         body: { template, service_description?, service_name? }
```

### Variáveis de Template Suportadas
`{{nome}}`, `{{bio}}`, `{{plataforma}}`, `{{service_description}}`

Substituição manual com `String.replace()` ou regex global antes de enviar ao Gemini.

### Nota da Story 2.1
`gemini.service.js` já está implementado. Esta story apenas adiciona:
1. Lógica de histórico no controller antes de chamar o Gemini
2. Suporte a `customPrompt` que bypassa o template padrão
3. Endpoints de leitura/escrita do `config.json`

### Testing
- Vitest para unit tests de `config.service.js`
- Supertest para integração dos novos endpoints
- Testar limite de 10 entradas no `message_history`

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-03-14 | 1.0 | Story criada | River (SM) |

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Debug Log References
- 54 testes totais passando (todos os anteriores + 4 regenerate + 3 config)
- Corrigido EADDRINUSE: servidor só inicia quando `NODE_ENV !== 'test'`

### Completion Notes List
- `message_history` limitado a 10 entradas com `slice(-MAX_HISTORY)`
- `customPrompt` faz bypass do template padrão (chama Gemini diretamente)
- Validação: template deve ser string não-vazia e conter `{{nome}}`
- Persistência do novo message só ocorre após sucesso do Gemini (não corrompe JSON em caso de falha)

### File List
- `prospect-hunter/server/src/services/config.service.js` — criado
- `prospect-hunter/server/src/controllers/config.controller.js` — criado
- `prospect-hunter/server/src/routes/config.routes.js` — criado
- `prospect-hunter/server/src/controllers/regenerate-message.controller.js` — criado
- `prospect-hunter/server/src/routes/leads.routes.js` — atualizado (POST /:id/regenerate-message)
- `prospect-hunter/server/src/index.js` — atualizado (/api/config, fix NODE_ENV guard)
- `prospect-hunter/server/tests/integration/config.api.test.js` — criado (3 testes)
- `prospect-hunter/server/tests/integration/regenerate-message.api.test.js` — criado (4 testes)

## QA Results

**Revisor:** Quinn (@qa) | **Data:** 2026-03-14 | **Decisão: CONCERNS**

### Resumo Executivo

Testes passam (54/54). Lógica de `message_history` correta (limite 10, append-only, persistência apenas pós-sucesso). Dois issues de tratamento de erros e uma violação de SoC precisam de correção.

### Issues Encontrados

| # | Severidade | Arquivo | Linha | Descrição |
|---|-----------|---------|-------|-----------|
| 1 | HIGH | `regenerate-message.controller.js` | L11, L19, L62 | `getById()`, `getPromptTemplate()` e `update()` sem try-catch. Erros de I/O não chegam ao `errorMiddleware` — resultam em unhandled promise rejection. |
| 2 | MEDIUM | `regenerate-message.controller.js` | L35-48 | `GoogleGenerativeAI` instanciado diretamente no controller para o caminho `customPrompt`. Duplica lógica de `gemini.service.js` (timeout, config de model). Deve ser extraído para `gemini.service.generateCustomMessage(prompt)`. |
| 3 | LOW | `error.middleware.js` | L3-7 | Mensagens de erro internas do SDK (Gemini/Apify) são retornadas diretamente ao cliente. Sem filtragem para produção — pode vazar detalhes internos de infraestrutura. |

### O que está correto

- `message_history`: append correto + limite via `slice(-MAX_HISTORY)`
- `customPrompt` bypass funciona e não persiste template
- Persistência de message e history só após sucesso do Gemini (atomicidade)
- Validação do template no PUT (`{{nome}}` obrigatório)
- Testes cobrem todos os 7 ACs

### Itens para Correção (@dev)

1. **[HIGH]** Envolver operações de I/O (`getById`, `getPromptTemplate`, `update`) em try-catch com `next(err)` em `regenerate-message.controller.js`
2. **[MEDIUM]** Mover lógica Gemini inline (L35-48) para `gemini.service.js` como função `generateCustomMessage(customPrompt)` — reutilizar `callGeminiWithTimeout()`
3. **[LOW / DEBT]** `error.middleware.js`: em `NODE_ENV === 'production'`, retornar mensagem genérica para erros sem `code` interno

### Decisão

**CONCERNS** — Aprovado condicionalmente. Issues #1 e #2 devem ser corrigidos antes do merge para produção. Issue #3 pode ir como tech debt.
