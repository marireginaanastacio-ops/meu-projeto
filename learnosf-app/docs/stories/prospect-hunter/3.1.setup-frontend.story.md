# Story 3.1: Setup Frontend e Listagem de Leads

## Status
Done

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "vite-build"]

## Story
**As a** usuário,
**I want** ver todos os meus leads em uma lista com informações básicas,
**so that** tenha uma visão geral da minha base de prospects assim que abrir o dashboard.

## Acceptance Criteria
1. Projeto React + Vite + Tailwind CSS + shadcn/ui configurado e funcionando em `:5173`
2. Proxy Vite configurado: requisições para `/api` redirecionadas para `http://localhost:3001`
3. `services/api.js` implementado como wrapper de `fetch` para todos os endpoints do backend
4. Hook `useLeads()` implementado: busca leads via `GET /api/leads`, gerencia estado de loading/error
5. Página principal (`LeadsPage.jsx`) exibe lista de leads com cards contendo: nome, badge de plataforma colorido (rosa=Instagram, azul=LinkedIn), bio truncada (max 100 chars), badge de status colorido, data de captura formatada (pt-BR)
6. Estado vazio exibe mensagem "Nenhum lead encontrado" com instrução para realizar primeira busca
7. Interface completamente em português brasileiro
8. Build `npm run build` completa sem erros

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- **Primary Type:** Frontend
- **Secondary Type(s):** Architecture (setup)
- **Complexity:** Medium — setup completo + componentes base + integração API

**Specialized Agent Assignment:**
- Primary Agents: @dev, @ux-expert
- Supporting Agents: —

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): Build sem erros, proxy configurado, sem console.log em produção
- [ ] Pre-PR (@ux-expert): Revisar acessibilidade básica dos componentes (alt texts, labels)

**Self-Healing Configuration:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Severity Filter: CRITICAL
- CRITICAL: auto_fix
- HIGH: document_only

**CodeRabbit Focus Areas:**
- Primary: Acessibilidade WCAG AA básica (labels, contraste), sem hardcoded URLs
- Secondary: Componentes reutilizáveis, Tailwind classes organizadas

## Complexity
**T-shirt:** M | **Pontos:** 5

## Business Value
Torna o produto utilizável: sem interface visual, o usuário precisaria usar `curl` para ver seus leads. Este é o primeiro momento em que o produto se torna tangível e usável.

## Scope
**IN:**
- Setup React + Vite + Tailwind + shadcn/ui
- Proxy Vite para backend
- `services/api.js` com todos os métodos
- Hook `useLeads`
- `LeadCard` e `LeadList` com estado vazio e loading
- Roteamento básico

**OUT (não inclui esta story):**
- Filtros de busca (Story 3.2)
- Painel de detalhe do lead (Story 3.3)
- Dashboard de métricas (Story 3.4)
- Formulário de nova busca (Story 3.4)

## Dependencies
- **Requer Story 1.3** — `GET /api/leads` deve estar disponível para a lista carregar dados reais
- **Requer Story 1.1** — estrutura do projeto deve existir para o frontend ser criado dentro do monorepo

## Tasks / Subtasks

- [x] 1. Configurar Tailwind CSS e shadcn/ui (AC: 1)
  - [x] Instalar e configurar Tailwind CSS + PostCSS no `client/`
  - [x] Instalar shadcn/ui: dependências manuais (class-variance-authority, clsx, tailwind-merge, tailwindcss-animate, lucide-react)
  - [x] Adicionar componentes: Badge, Card, Button, Skeleton
- [x] 2. Configurar proxy Vite (AC: 2)
  - [x] Atualizar `client/vite.config.js` com proxy `/api` → `http://localhost:3001`
- [x] 3. Implementar `services/api.js` (AC: 3)
  - [x] Criar `client/src/services/api.js`
  - [x] Função base `apiFetch(path, options)` com tratamento de erros
  - [x] Funções específicas: `getLeads(params)`, `getLead(id)`, `updateLead(id, data)`, `deleteLead(id)`, `generateMessage(id)`, `regenerateMessage(id, customPrompt)`, `searchProspects(params)`, `getStats()`, `getConfig()`, `updateConfig(data)`
- [x] 4. Implementar `useLeads` hook (AC: 4)
  - [x] Criar `client/src/hooks/useLeads.js`
  - [x] Estados: `leads`, `loading`, `error`, `total`
  - [x] Função `refetch(params)` para recarregar com filtros
  - [x] Fetch inicial no mount
- [x] 5. Criar componentes de Lead (AC: 5, 6)
  - [x] Criar `client/src/components/leads/LeadCard.jsx`:
    - Nome em destaque
    - Badge plataforma: rosa (#E1306C) = Instagram, azul (#0077B5) = LinkedIn
    - Bio truncada com CSS line-clamp ou JS slice
    - Badge status com cores: cinza=Novo, amarelo=Contatado, azul=Respondeu, verde=Convertido, vermelho=Descartado
    - Data formatada: `new Intl.DateTimeFormat('pt-BR').format(date)`
  - [x] Criar `client/src/components/leads/LeadList.jsx`:
    - Renderiza array de `LeadCard`
    - Estado vazio com mensagem e ícone
    - Loading state com Skeleton cards
- [x] 6. Criar `LeadsPage.jsx` (AC: 5, 7)
  - [x] Criar `client/src/pages/LeadsPage.jsx`
  - [x] Usar `useLeads` hook
  - [x] Renderizar `LeadList` com dados
- [x] 7. Configurar `App.jsx` e roteamento básico (AC: 7, 8)
  - [x] Instalar `react-router-dom`
  - [x] Rota `/` → `LeadsPage`
  - [x] Layout base com header simples "Prospect Hunter"
- [x] 8. Validar build (AC: 8)
  - [x] `npm run build` sem erros (✓ built in 4.05s, 200KB JS + 12KB CSS)
  - [ ] Confirmar que a lista carrega leads do backend (validação manual)

## Dev Notes

### Estrutura de Componentes (Source: architecture/prospeccao-automatizada-architecture.md#2)

```
client/src/
├── components/
│   ├── leads/
│   │   ├── LeadList.jsx
│   │   ├── LeadCard.jsx
│   │   ├── LeadDetail.jsx     (Story 3.3)
│   │   └── LeadFilters.jsx    (Story 3.2)
│   ├── dashboard/             (Story 3.4)
│   └── ui/                    (shadcn/ui customizados)
├── hooks/
│   ├── useLeads.js
│   ├── useStats.js            (Story 3.4)
│   └── useSearch.js           (Story 3.2)
├── services/
│   └── api.js
└── pages/
    ├── LeadsPage.jsx
    └── DashboardPage.jsx      (Story 3.4)
```

### Cores por Plataforma e Status (Source: prd/prospeccao-automatizada-PRD.md#UI)
- Instagram badge: `bg-pink-500 text-white`
- LinkedIn badge: `bg-blue-700 text-white`
- Status cores:
  - Novo: `bg-gray-100 text-gray-700`
  - Contatado: `bg-yellow-100 text-yellow-700`
  - Respondeu: `bg-blue-100 text-blue-700`
  - Convertido: `bg-green-100 text-green-700`
  - Descartado: `bg-red-100 text-red-700`

### API Wrapper Pattern
```javascript
// client/src/services/api.js
const BASE_URL = '/api';

async function apiFetch(path, options = {}) {
  const res = await fetch(`${BASE_URL}${path}`, {
    headers: { 'Content-Type': 'application/json' },
    ...options
  });
  const data = await res.json();
  if (!data.success) throw new Error(data.error);
  return data;
}

export const getLeads = (params) => apiFetch(`/leads?${new URLSearchParams(params)}`);
export const getStats = () => apiFetch('/leads/stats');
// ... outros métodos
```

### Testing
- Nesta story, foco em validação manual (build, UI visual)
- Sem testes automatizados de componentes React no MVP (conforme NFR — sem e2e)

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-03-14 | 1.0 | Story criada | River (SM) |

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Debug Log References
- Build bem-sucedido: ✓ 1759 modules, 200.25 kB JS, 12.18 kB CSS, built in 4.05s
- Fix aplicado: `tailwind.config.js` usava `await import()` que jiti (Tailwind v3) não suporta — corrigido para `import animatePlugin from 'tailwindcss-animate'`

### Completion Notes List
- shadcn/ui configurado manualmente (sem CLI interativo): dependências base + componentes Badge, Card, Button, Skeleton criados em `components/ui/`
- `lib/utils.js` com função `cn()` (clsx + tailwind-merge)
- Proxy Vite `/api → :3001` já estava configurado da Story 1.1
- react-router-dom já estava instalado da Story 1.1
- `LeadCard` com acessibilidade: role="button", tabIndex=0, aria-label, onKeyDown para Enter
- `LeadList` com aria-busy e aria-label no loading state
- `LeadsPage` com role="alert" no estado de erro
- Build sem warnings nem erros

### File List
- `prospect-hunter/client/tailwind.config.js` — atualizado (darkMode, animatePlugin)
- `prospect-hunter/client/src/index.css` — atualizado (CSS variables)
- `prospect-hunter/client/src/lib/utils.js` — criado (cn helper)
- `prospect-hunter/client/src/components/ui/badge.jsx` — criado
- `prospect-hunter/client/src/components/ui/card.jsx` — criado
- `prospect-hunter/client/src/components/ui/button.jsx` — criado
- `prospect-hunter/client/src/components/ui/skeleton.jsx` — criado
- `prospect-hunter/client/src/services/api.js` — criado
- `prospect-hunter/client/src/hooks/useLeads.js` — criado
- `prospect-hunter/client/src/components/leads/LeadCard.jsx` — criado
- `prospect-hunter/client/src/components/leads/LeadList.jsx` — criado
- `prospect-hunter/client/src/pages/LeadsPage.jsx` — criado
- `prospect-hunter/client/src/App.jsx` — atualizado (BrowserRouter + Layout + Routes)

## QA Results

### Revisão: @qa (Quinn) — 2026-03-14

**Veredicto: PASS**

---

#### 7 Quality Checks

| # | Check | Resultado | Observações |
|---|-------|-----------|-------------|
| 1 | Code review (padrões, legibilidade) | ✅ PASS | Código limpo, sem console.log, sem URLs hardcoded, tratamento de erros consistente |
| 2 | Testes unitários | ✅ WAIVED | Dev Notes documentam: sem testes de componentes React no MVP (validação manual). 54 testes backend passando — sem regressões |
| 3 | Critérios de aceitação | ✅ PASS | Todos os 8 ACs verificados e atendidos (ver detalhe abaixo) |
| 4 | Sem regressões | ✅ PASS | 54 testes backend passando; nenhum arquivo de backend modificado |
| 5 | Performance | ✅ PASS | Build: 200KB JS / 64KB gzip — aceitável para MVP React 18 + router |
| 6 | Segurança (OWASP básico) | ✅ PASS | Sem hardcoded credentials; sem dangerouslySetInnerHTML; BASE_URL relativo; erros exibidos como texto puro |
| 7 | Documentação | ✅ PASS | Dev Agent Record completo; decisões técnicas documentadas em project_context.md |

---

#### Verificação dos Acceptance Criteria

| AC | Descrição | Status |
|----|-----------|--------|
| AC1 | React + Vite + Tailwind + shadcn/ui em `:5173` | ✅ Confirmado (build 1759 modules) |
| AC2 | Proxy Vite `/api → http://localhost:3001` | ✅ Confirmado em `vite.config.js` |
| AC3 | `services/api.js` com todos os endpoints | ✅ 10 funções cobrindo todos os endpoints da API |
| AC4 | Hook `useLeads()` com loading/error/total/refetch | ✅ Implementado com useCallback |
| AC5 | LeadCard com badges, bio truncada, data pt-BR | ✅ Cores corretas (rosa Instagram, azul LinkedIn), fallback para plataformas desconhecidas |
| AC6 | Estado vazio "Nenhum lead encontrado" | ✅ Mensagem + ícone + instrução de próximo passo |
| AC7 | Interface em português brasileiro | ✅ Todos os textos em pt-BR |
| AC8 | Build sem erros | ✅ `built in 3.71s`, zero warnings, zero erros |

---

#### Concerns Documentados (LOW — não bloqueantes)

1. **`useLeads.js:26`** — `eslint-disable-line react-hooks/exhaustive-deps`: fetch intencional apenas no mount (initialParams fora das deps). Correto para o MVP, mas pode confundir futuros desenvolvedores. Recomendação: comentar a intenção inline.
2. **`LeadList.jsx`** — prop `onLeadClick` recebido mas passado como `onClick` para `LeadCard`. Prop drilling simples e intencional para Story 3.3 (painel de detalhe). Nenhuma ação necessária.

**Ação:** Ambos registrados como LOW — documentados para Stories 3.3 e além. Não bloqueiam o merge.

---

#### Validação de Segurança

- Sem `console.log` em produção ✅
- Sem `dangerouslySetInnerHTML` ✅
- Sem URLs hardcoded no `src/` (BASE_URL = '/api' relativo) ✅
- Sem credenciais ou tokens expostos ✅
- Erros do backend exibidos como texto, não como HTML ✅

---

**Decisão final: PASS — Story 3.1 aprovada para merge na `main`.**
